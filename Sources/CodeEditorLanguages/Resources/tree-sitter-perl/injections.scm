; No validated injection patterns for the pinned perl grammar pin.
; Keep a compiling empty-safe query surface: match nothing via impossible predicate.
; (File must compile when present — LANG-N02/N06 fail closed.)
